/// Set to `true` to run without Firebase (UI demo / emulator presentation).
/// Set to `false` after Firebase is configured and dependencies are restored.
class AppConfig {
  AppConfig._();

  static const bool demoMode = true;

}
